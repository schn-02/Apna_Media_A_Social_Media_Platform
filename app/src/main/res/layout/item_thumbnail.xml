<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardBackgroundColor="#100817"
    app:cardCornerRadius="18dp"
    app:cardElevation="5dp"
    app:strokeColor="#33203D"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="118dp"
        android:layout_height="118dp">

        <!-- Thumbnail Image -->
        <ImageView
            android:id="@+id/thumbnailImageView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="#08040C"
            android:scaleType="centerCrop"
            android:src="@drawable/user"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/user" />

        <!-- Bottom Fade Overlay -->
        <View
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_marginBottom="0dp"
            android:background="@drawable/bg_thumbnail_bottom_fade"
            app:layout_constraintBottom_toBottomOf="@id/thumbnailImageView"
            app:layout_constraintEnd_toEndOf="@id/thumbnailImageView"
            app:layout_constraintStart_toStartOf="@id/thumbnailImageView" />

        <!-- Unselected Circle -->
        <ImageView
            android:id="@+id/circlePost"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_thumbnail_unselected_circle"
            android:padding="7dp"
            android:src="@drawable/circlepost"
            android:visibility="visible"
            app:layout_constraintEnd_toEndOf="@id/thumbnailImageView"
            app:layout_constraintTop_toTopOf="@id/thumbnailImageView"
            app:tint="#FFFFFF" />

        <!-- Selected Count Badge -->
        <TextView
            android:id="@+id/imageNumberOverlay"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_thumbnail_selected_badge"
            android:gravity="center"
            android:includeFontPadding="false"
            android:text="1"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="@id/thumbnailImageView"
            app:layout_constraintTop_toTopOf="@id/thumbnailImageView"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>