<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="118dp"
    android:layout_height="118dp"
    android:layout_margin="3dp"
    app:cardBackgroundColor="#100817"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeColor="#452750"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_view_profile_post_item">

        <!-- Post Image -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/viewProfileImage"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="#08040C"
            android:scaleType="centerCrop"
            android:src="@drawable/picture"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:shapeAppearanceOverlay="@style/ViewProfilePostImageShape"
            tools:src="@drawable/picture" />

        <!-- Top Gradient Overlay -->
        <View
            android:layout_width="0dp"
            android:layout_height="46dp"
            android:background="@drawable/bg_view_profile_top_fade"
            app:layout_constraintEnd_toEndOf="@id/viewProfileImage"
            app:layout_constraintStart_toStartOf="@id/viewProfileImage"
            app:layout_constraintTop_toTopOf="@id/viewProfileImage" />

        <!-- Multiple Photos Badge -->
        <FrameLayout
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_view_profile_photo_badge"
            app:layout_constraintEnd_toEndOf="@id/viewProfileImage"
            app:layout_constraintTop_toTopOf="@id/viewProfileImage">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_gravity="center"
                android:src="@drawable/photos" />

        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>